## 📂 제7장 자료구조 II (튜플, 세트, 딕셔너리)
* **튜플 (Tuple)**: 리스트와 같지만 한 번 만들면 **수정·삭제 불가능** `( )`.
* **세트 (Set)**: **중복을 허용하지 않고** 순서가 없는 구조 `{ }`. (집합 연산 가능)
* **딕셔너리 (Dictionary)**: **키(Key)와 값(Value)**의 쌍으로 데이터를 매칭하는 구조 `{ Key: Value }`.
* **문자열 처리**: `split()` 메서드로 특정 기준에 따라 문자열을 쪼개 리스트로 반환.
