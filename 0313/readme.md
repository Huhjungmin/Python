## 📂 제2장 변수와 수식
* **변수와 상수**: **변수**는 데이터를 저장하는 공간(값 변경 가능), **상수**는 고정된 수.
* **산술 연산자**: `//` (**몫**), `%` (**나머지**), `**` (**거듭제곱**).
* **입출력/형변환**: `input()`은 **항상 문자열**로 입력받으므로, 계산하려면 `int()`나 `float()`로 바꿔야 함.
